# WDS Project Outline
# Generated by wds-0-project-setup (Phase 0)
# Date: 2026-08-24

project_name: "Pokémon TCG Marketplace and Collection Management Platform"
project_slug: pokemon-tcg-marketplace
project_type: greenfield          # Phase 1-7 path (not Phase 8 / Brownfield)
alignment_needed: false           # ready to start, no pitch/signoff step required

product_complexity: complex       # C: Web Application -> full workflow, all phases
tech_stack: null                  # deliberately skipped at this stage (Non-Goal per SPEC.md)
component_library: custom
skip_design_system: false         # Phase 5 / D-Design-System recommended for "custom"

# Root of all WDS phase output. NOTE: this project already had BMad installed
# with output_folder fixed to {project-root}/_bmad-output in _bmad/wds/config.yaml
# and _bmad/core/config.yaml — every downstream phase skill reads from there,
# so root_folder is NOT a free choice here; it mirrors the existing install config
# rather than creating a separate design-process/ or docs/ tree.
root_folder: "_bmad-output"

brief_level: complete             # Greenfield always uses Complete Brief
strategic_analysis: simplified    # Full Trigger Map (Phase 2) explicitly skipped per
                                   # the course exercise handout; strategic context
                                   # folds into the Phase 1 brief instead

existing_materials:
  has_materials: true
  items:
    - path: "docs/Problem-statement.pdf"
      description: "Original course problem statement (Martín Gómez & Mateo Rubio)"
    - path: "docs/spec-task-1/SPEC.md"
      description: "Spec Task 1 deliverable: Contract, Capabilities, Constraints, Non-Goals, Success Signal, Assumptions"
    - path: "docs/spec-task-1/JUSTIFICATION.md"
      description: "Decisions, AI-usage tracking, and self-review behind the Spec Task 1 deliverable"
    - path: "docs/spec-task-1/01-human-draft/SPEC.md"
      description: "Stage-1 human-only draft, pre-AI-assistance"
    - path: "docs/Martin's specification peer review.pdf"
      description: "Adversarial peer review of Spec Task 1 (clarity/scope/consistency findings, unflagged assumptions D1-D5)"
    - path: "_bmad-output/specs/spec-pokemon-tcg-marketplace/"
      description: "Standalone bmad-spec skill run: SPEC.md, stakeholders.md, .memlog.md (51 append-only entries)"

project_context:
  stakes: small_business_investment   # balanced rationale, professional tone
  involvement: balanced
  role: product_owner
  recommendation_style: recommend_with_rationale

working_relationship:
  stakes: small_business_investment
  involvement: balanced
  role: product_owner
  recommendation_style: recommend_with_rationale

phases_enabled:
  phase_1_product_brief: true
  phase_2_trigger_mapping: false   # skipped per handout annotation ("not needed in this project")
  phase_3_ux_scenarios: true       # not in scope for this exercise, but not disabled
  phase_4_ux_design: true
  phase_5_agentic_development: true
  phase_6_asset_generation: true
  phase_7_design_system: true

# This exercise's actual target sequence (per course handout):
# wds-0-project-setup -> wds-agent-saga-analyst -> wds-1-project-brief
#   -> bmad-spec -> bmad-architecture
# Trigger Mapping (Phase 2) is explicitly out of scope. bmad-spec/bmad-architecture
# are separate skills (not Phase 3+) that write to _bmad-output/specs/spec-{slug}/.

created: "2026-08-24"
