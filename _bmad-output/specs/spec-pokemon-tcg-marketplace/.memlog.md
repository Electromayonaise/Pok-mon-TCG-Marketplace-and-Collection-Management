---
topic: Pokémon TCG Marketplace and Collection Management Platform — whole-system spec (Colombia)
updated: 2026-08-25T16:29
---

- (note by ai) Source: docs/Problem-statement.pdf (student brief, Martin Gomez & Mateo Rubio). Scope: first-iteration, whole-system, stakeholder-level spec; architecture/DB/UI/stack explicitly out of scope per source.
- (capability by ai) CAP-1 intent: User can browse and filter the card/product catalog by set, era, Pokemon, color, style, and artist. success: A filtered query returns only catalog entries matching all applied filters, including entries with zero active listings.
- (capability by ai) CAP-2 intent: Catalog entries exist independently of marketplace listings; a listing links to a catalog entry but is never required for the entry to exist. success: A card with no listings is still retrievable from the catalog and displays hasActiveListings=false rather than being absent or erroring.
- (capability by ai) CAP-3 intent: User can see current listing price, most recent transaction price, and historical price trend for a card as distinct values. success: The card detail view renders three separately labeled price values that never collapse into one number.
- (capability by ai) CAP-4 intent: Businesses and individual sellers can publish cards, card bundles, and sealed products as listings. success: A seller of either type can create a listing tied to a valid catalog entry or bundle, and it becomes visible to buyers.
- (capability by ai) CAP-5 intent: The platform applies different registration and transaction rules to verified businesses vs individual sellers. success: A business listing cannot display as verified until its application status is Approved; an individual-seller listing never requires that state.
- (capability by ai) CAP-6 intent: A buyer can generate a pre-filled external-channel contact message (with product and listed price) to reach an individual seller. success: Selecting contact-seller on an individual-seller listing produces message text containing the product name and listed price, ready to send via an external messaging app.
- (capability by ai) CAP-7 intent: The platform supports user profiles, business profiles, and a review-based reputation mechanism. success: A profile page displays accumulated reviews/ratings tied to that seller or business account.
- (capability by ai) CAP-8 intent: User can add a card or product to one or more personal collections regardless of whether it was acquired through the platform. success: A manually-added item and a platform-purchased item can both appear in the same collection, each tagged with its acquisition source.
- (capability by ai) CAP-9 intent: User can see the value of a collection and how it has changed over time. success: A collection view exposes a current total value and a trend over a prior period, derived from constituent item prices.
- (capability by ai) CAP-10 intent: User can view a collection as a virtual binder organized to their own preference. success: A binder view renders the collection's items in a user-chosen layout/order rather than one fixed system ordering.
- (capability by ai) CAP-11 intent: User can create multiple named collection groupings (e.g., general, sealed products, keep, for-sale). success: A user can create at least two distinct collections and move/copy an item's membership between them.
- (capability by ai) CAP-12 intent: The platform distinguishes items acquired through the platform from items added manually by the user. success: Every collection entry carries a source flag (PlatformPurchase or Manual) visible wherever the entry is displayed.
- (capability by ai) CAP-13 intent: User can maintain a wish list / acquisition-planning list of cards they want to obtain, separate from owned collections. success: Adding a card to the wish list does not create a collection entry, and the two lists are independently viewable.
- (capability by ai) CAP-14 intent: User can plan acquisitions using price, availability, and seller/product location as factors. success: Wish-list items can be filtered or sorted by at least price and availability against current listings.
- (capability by ai) CAP-15 intent: An admin can review, approve, or reject pending business applications. success: A submitted application is queryable in a Pending state until an admin action transitions it to Approved or Rejected, and only Approved businesses are represented as verified.
- (capability by ai) CAP-16 intent: A seller creating a bundle identifies its component cards so the system can associate the bundle with those catalog entities. success: A bundle record lists its component card references, and each component remains independently visible/queryable in the catalog.
- (constraint by ai) Catalog/listing independence: a card's presence in the catalog must never be derived from or gated by the existence of a listing. Rules out implementing the catalog as a view over listings.
- (constraint by ai) Price provenance: listing price, recent transaction price, and historical market value must be stored and displayed as distinct fields, never merged or treated as interchangeable.
- (constraint by ai) Individual sellers require no business-style verification to publish listings; only businesses go through the legal-identity + external-presence application process. Rules out a single uniform seller-onboarding flow.
- (constraint by ai) Business applications remain Pending until an admin approves them; a business cannot be represented as verified before approval. Rules out auto-approval.
- (constraint by ai) Individual-seller transactions are not necessarily processed by the platform; the platform's role for that path ends at generating a contact message for an external channel. Rules out assuming the platform always owns checkout.
- (constraint by ai) A bundle must track its component cards individually so bundle contents remain associated with catalog entities. Rules out treating a bundle as an opaque single SKU.
- (constraint by ai) Collection ownership is independent of platform purchase; manual addition of externally-acquired items must be supported. Rules out restricting collections to platform-sourced items only.
- (constraint by ai) The system must support multiple, user-chosen ways of organizing a collection (date, value, set, Pokemon, artist, color, binder layout) rather than one fixed taxonomy. Rules out a single mandated organizing scheme.
- (constraint by ai) Course constraint: this is the first-iteration, system-level spec; it must not fix internal module decomposition, database schema, UI/visual design, deployment architecture, or technology stack (source: Problem-statement.pdf, Out of Scope for the First Iteration).
- (decision by ai) Non-goal: final internal module split of the platform is out of scope for this spec.
- (decision by ai) Non-goal: exact database schema and data model are out of scope for this spec.
- (decision by ai) Non-goal: exact user interface and visual design are out of scope for this spec.
- (decision by ai) Non-goal: final deployment architecture is out of scope for this spec.
- (decision by ai) Non-goal: exact technology stack is out of scope for this spec.
- (decision by ai) Non-goal: definitive implementation of external integrations (external messaging app, Instagram, external Pokemon TCG data sources) is out of scope for this spec.
- (decision by ai) Non-goal: the exact price-estimation or market-value calculation algorithm is out of scope for this spec.
- (decision by ai) Non-goal: final business model, commissions, or monetization strategy are out of scope for this spec.
- (decision by ai) Non-goal: detailed logistics and physical delivery process for marketplace transactions are out of scope for this spec.
- (decision by ai) Non-goal: in-platform dispute resolution between buyers and sellers is out of scope; not mentioned in source and would require mechanisms (arbitration, refunds) the source never specifies.
- (assumption by ai) Risky: assumed the external messaging channel for individual-seller contact is a consumer chat app (e.g., WhatsApp) reachable from a generated deep link or copyable message; source names 'any messaging app' without specifying integration depth.
- (assumption by ai) Risky: assumed catalog seed data (card/set metadata, artwork references) will come from an existing external Pokemon TCG data source rather than being manually authored by the team; source treats the catalog as pre-existing structured data without naming its origin.
- (assumption by ai) Risky: assumed a single seller account is exclusively either an individual seller or a verified business at a given time, not both simultaneously; source describes them as two distinct interaction models without addressing dual-role accounts.
- (assumption by ai) Safe: assumed business-application review is a manual human admin action rather than automated approval, matching source's description of admins reviewing/approving applications.
- (assumption by ai) Safe: assumed users access the platform via internet-connected web or mobile clients; no offline mode implied anywhere in source.
- (assumption by ai) Safe: assumed each user can own multiple independent collections (not capped at one), consistent with source's explicit 'one or more personal collections' language.
- (question by ai) Open: for business-seller listings, does the platform process the transaction/payment directly, or is it also handed off externally like individual sellers? Source only specifies the external-handoff model for individual sellers and is silent on the business-seller transaction path.
- (question by ai) Open: what currency (or currencies) are prices denominated in? Source establishes a Colombia focus but never states currency; this affects the Contract's price fields.
- (question by ai) Open: who is eligible to leave a review on a seller/business (any user, or only users with a completed transaction)? Source mentions 'reviews' as a reputation mechanism without eligibility rules, which affects trust/anti-fraud design.
- (question by ai) Open: can a single card be simultaneously part of an active bundle and individually listed by the same seller, and if so how does inventory/quantity reconcile between the two? Source requires bundle-to-component linkage but does not address concurrent individual listing of the same card.
- (question by ai) Open: can a listing carry seller-specific attributes (e.g., condition, grading, language) distinct from the catalog's canonical card identity, or is the catalog entry itself the only source of such detail? Source distinguishes catalog vs listings conceptually but does not enumerate listing-level fields.
- (question by ai) Open: what specific actions can a platform administrator take on user-generated content (listings, reviews) beyond business-application approval - e.g., can they edit, hide, or only remove? Source says admins 'moderate listings and user-generated information' without enumerating moderation actions.
- (question by ai) Open: is there any limit on how many collections, wish lists, or bundles a single user/seller can create? Source is silent on volume limits, which affects whether abuse/resource constraints need addressing even at spec level.
- (decision by ai) Why: Colombian Pokemon TCG collectors, buyers, individual sellers, and businesses currently split card research, price-checking, seller discovery, negotiation, and collection tracking across unrelated tools (social media, chat apps, foreign price trackers), forcing manual reconciliation of card identity, price, and ownership across sources. A centralized platform benefits collectors/buyers (one consistent view of catalog+price+listing+collection) and individual sellers/small businesses (a single place to be discovered without building a storefront). Grounded directly in the Problem Statement and Stakeholders sections of the source, not a hypothetical.
- (decision by ai) Success signal: the platform is working as specified when a user can, in one session, find a card in the catalog regardless of listing status, see its price context (listing/recent-sale/historical as distinct values), reach a seller (individual via generated external message, or business once approved) and record the item into a personal collection with correct source attribution (PlatformPurchase vs Manual) - without needing a second external tool to reconcile any of those facts.
- (constraint by ai) The platform must establish clear boundaries around user-generated content, seller information, reviews, listings, and market data so the reliability and consistency of information presented to users can be maintained. Rules out treating all user-submitted data as equally authoritative.
- (constraint by ai) Business applications require legal identity and external-presence information (e.g., Instagram profile or website) as submitted fields. Rules out approving a business on listing content alone.
- (event by ai) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS. All 16 capabilities have intent+success; intents are WHAT not HOW; constraints each rule something out; 10 non-goals present; success signal is a demonstrable end-to-end scenario; CAP IDs unique/stable; prose lean, no hedges.
- (event by ai) Self-validate pass 2 (preservation): found 3 gaps on first sweep - (a) Stakeholders section (6 actor groups with distinct goals) does not fit the 5-field kernel, spawning companion stakeholders.md; (b) user-generated-content reliability boundary from Constraints-and-Tensions was unabsorbed, added as constraint; (c) business-application required fields (legal identity, external presence) were unabsorbed, added as constraint. All 3 fixed this pass; re-swept clean. No wrapper-only content dropped - Context, Problem Statement, Project Intention, and Expected First Deliverable sections are framing/meta and were traced into Why + companion, not separately preserved as they carry no additional load-bearing claims beyond what's captured.
- (note by ai) Session 2 update. Sources: docs/spec-task-1/SPEC.md + JUSTIFICATION.md (human-reconciled, six-stage spec-task-1 deliverable, treated as current fact) and _bmad-output/A-Product-Brief/01-product-brief.md + dialog/decisions.md (WDS Phase 1, Saga-run product brief). This update catches the memlog up to spec-task-1's stage 4-7 human resolutions (not reflected here since this memlog predates them) and layers new capability signals from the Product Brief on top. wds-2-trigger-mapping explicitly out of scope for this project.
- (decision by ai) Catch-up from spec-task-1 stage 4: business-seller purchases are in-platform and mandatory, not externally handed off like individual-seller sales (source: JUSTIFICATION.md Key Decision 6a). Resolves prior open question on business-transaction processing.
- (capability by ai) CAP-17 intent: A buyer can purchase a verified-business listing directly through the platform. success: Purchasing a verified-business listing returns an OrderId and decrements listing quantity; the same action against an individual-seller listing is rejected (NotBusinessListing) rather than silently succeeding.
- (decision by ai) Catch-up: reference prices (last-transaction, historical trend) are sourced externally in USD and displayed as a USD/COP pair; seller-set listing prices are COP-only (JUSTIFICATION.md Key Decision 6b). Resolves prior open currency question.
- (decision by ai) CAP-3 success updated: the card detail view's three price values (listing/last-transaction/historical-trend) render with listing price in COP and the two reference prices as USD/COP pairs, never a single unlabeled number.
- (decision by ai) Catch-up: verified businesses are reachable via in-app messaging; individual sellers deliberately are not, to keep their transactions off-platform by design (JUSTIFICATION.md Key Decision 6c).
- (capability by ai) CAP-18 intent: A user can send an in-app message to a verified business. success: A message sent to a verified business is delivered in-app; the same action against a non-business account is rejected (NotBusinessAccount).
- (constraint by ai) In-app messaging exists only for verified businesses; individual-seller listings are reachable only via the generated external-contact message (CAP-6), never in-app. Rules out a single unified messaging channel for both seller types.
- (decision by ai) Catch-up: reviews on a verified business require a completed platform purchase (NotVerifiedPurchaser gate); individual-seller reviews keep the platform-wide no-gate default, explicitly flagged as an abuse-risk assumption rather than silently decided (JUSTIFICATION.md Key Decision 6d).
- (constraint by ai) A review targeting a verified business requires the reviewer to have a completed platform purchase against that business; no equivalent purchase gate exists for individual-seller reviews.
- (assumption by ai) Risky: individual-seller reviews default to the platform-wide no-gate baseline (no purchase-verification requirement), since off-platform transactions can't be confirmed by the platform. Carried forward from spec-task-1; still genuinely undecided, not newly resolved by the Product Brief.
- (decision by ai) Catch-up: a first-time individual seller completes a short profile step, short of business-style verification, before their first listing (JUSTIFICATION.md Key Decision 6e). Exact fields collected remain unspecified - kept as a Non-goal, not guessed.
- (capability by ai) CAP-19 intent: A first-time individual seller completes a short profile step before creating their first listing. success: Attempting to list as an individual seller who hasn't completed the profile step errors with IndividualSellerProfileIncomplete; a business account is exempt from this specific gate (it has its own verification flow).
- (constraint by ai) A listing may carry a seller-specific condition attribute distinct from the catalog entry's canonical identity (e.g. CreateListing's optional condition field). Rules out the catalog entry being the only source of listing-level detail.
- (decision by ai) CAP-9 success updated: GetCollectionValueHistory's totalValue is COP-denominated, consistent with all other collection-facing price fields (spec-task-1 stage-5 adversarial-review finding).
- (assumption by ai) Risky: a successful PurchaseListing does not automatically create a collection entry - AddToCollection is a separate, buyer-initiated action. Carried forward from spec-task-1 stage 5; unresolved, directly affects the Why section's 'one consistent view' claim if false.
- (assumption by ai) Risky: bundle/individual-listing inventory reconciliation for the same underlying card, and the individual-seller-to-verified-business account transition (what happens to existing individual listings), are both unaddressed by any source to date. Carried forward from spec-task-1 stage 5/6, merged into one flag there; kept as one entry here for the same reason.
- (constraint by ai) Review anti-abuse (self-review, collusion) has no safeguard beyond the business purchase-verification gate. Explicit Non-goal, not a silent gap (spec-task-1 stage 5).
- (note by ai) Product Brief signals absorbed below (dialog/decisions.md Decisions 4, 6-10, 12-13): location/distance filtering, add-to-binder-via-link, no-payment-gateway P2P settlement, prepaid commission balance, three-state delivery confirmation without escrow, and a trading system for individual sellers. All new capability areas not present in spec-task-1's Contract.
- (decision by ai) Location/distance filtering: casual individual sellers are discouraged by listing friction today; a distance/location filter would enable in-person pickup or local-seller preference (Product Brief Decision 6). Applied as a new filter dimension on the existing catalog/listing browse capability (CAP-1), not a separate capability.
- (decision by ai) CAP-1 intent updated: user can browse and filter the catalog by set, era, Pokemon, color, style, artist, and listing location/distance. success updated: a filtered query, including location/distance filters, returns only matching catalog entries/listings, including entries with zero active listings when no location filter narrows them out.
- (question by ai) Open: does location/distance filtering apply to all listings (individual and verified-business) or only individual-seller listings? Product Brief motivates it specifically for individual/local matching (in-person pickup) but does not exclude business listings; not decided here.
- (decision by ai) Add-to-binder-via-link: cards not yet indexed in the catalog (promos, errors, very new set releases) can still be added to a user's virtual binder via an external link/reference, per pkmnbindr.com's structure (Product Brief Decision 7).
- (capability by ai) CAP-24 intent: A user can add a card to their virtual binder via an external link/reference when it has no catalog entry yet. success: A link-added binder entry displays in the binder without requiring a matching catalog entry to exist.
- (constraint by ai) A binder entry does not require a corresponding catalog entry to exist (distinct from CAP-2's catalog/listing independence, which governs listings, not binder entries). Rules out the binder being strictly catalog-bound.
- (decision by ai) No payment gateway: a gateway (e.g. Wompi) would double-charge (transaction + disbursement fee) on funds the platform never holds, since it's a pass-through to businesses. Business purchases instead settle peer-to-peer: buyer pays the business directly via a QR code or bank account the business registers at verification, then uploads a comprobante and confirms 'I've paid'; the business confirms 'payment received' (Product Brief Decision 8). This is the mechanism CAP-17/PurchaseListing runs on - supersedes any implication of automated payment capture.
- (capability by ai) CAP-20 intent: A business purchase is settled by the buyer paying the business directly (QR/bank transfer) and uploading proof of payment, mutually confirmed by both parties. success: An order shows a buyer 'paid' state only after a comprobante is uploaded and the buyer confirms payment; it shows 'payment received' only after the business separately confirms.
- (constraint by ai) The platform never holds or disburses transaction funds for business purchases; settlement is peer-to-peer between buyer and business, verified by an uploaded comprobante and mutual confirmation, not an automated payment capture. Rules out any escrow or hold-and-release payment flow.
- (decision by ai) Prepaid commission balance: verified businesses top up a commission balance before their listings become purchasable; each confirmed sale auto-deducts commission from that balance. A zero balance pauses (not disables) the business's listings until topped up - removing collections risk entirely rather than managing it after the fact (Product Brief Decision 8).
- (capability by ai) CAP-21 intent: A verified business maintains a prepaid commission balance that gates whether its listings are purchasable. success: A business with a positive balance has purchasable listings that auto-deduct commission on each confirmed sale; a business whose balance reaches zero has its listings paused (still visible/editable, not purchasable) until the balance is topped up.
- (constraint by ai) Commission is collected as a prepaid balance deduction per confirmed sale, never as a post-hoc invoice. Rules out invoicing/billing-after-the-fact commission collection.
- (constraint by ai) A zero commission balance pauses a business's listings; it must not delete, hide, or ban the business account or its listing records. Rules out treating a lapsed balance as an account-level penalty.
- (decision by ai) Three-state delivery confirmation without escrow: since the platform never holds funds, it can't hold-and-release like a gateway. Instead: buyer-paid to business-received-payment to buyer-received-item are three distinct, visible states, and identity-backed reputation (business legal-identity verification + purchase-gated reviews) is the real enforcement lever against non-delivery. A forfeitable buyer-protection deposit was discussed as a possible v2 hardening, not adopted for v1 (Product Brief Decision 9).
- (capability by ai) CAP-22 intent: A business-purchase order tracks payment and fulfillment as three distinct, independently visible states rather than one collapsed 'complete' status. success: An order's state (paid / payment-received-by-seller / item-received-by-buyer) is individually queryable at each stage, and reaching 'item-received-by-buyer' is the only state that closes the order.
- (constraint by ai) Delivery assurance for business purchases relies on the three-state confirmation sequence plus identity-backed seller reputation, not fund-holding/escrow. Rules out any design that requires the platform to hold or release transaction funds.
- (decision by ai) Trading system for individual sellers: a listing explicitly marked 'open to trade' can receive trade offers combining cards, other product, and/or money in any mix; the seller accepts, rejects, or counter-offers. Negotiation happens on-platform; the actual exchange happens off-platform, matching the existing individual-seller external-handoff model. Only listings flagged 'open to trade' are eligible - not every individual-seller listing by default (Product Brief Decision 10).
- (capability by ai) CAP-25 intent: A buyer can propose a trade offer (cards, product, and/or money) against an individual-seller listing marked 'open to trade,' and the seller can accept, reject, or counter it. success: A trade offer against a listing not flagged 'open to trade' is rejected/unavailable; against an eligible listing, the seller's accept/reject/counter action is recorded and visible to the buyer, and an accepted trade hands off exchange details the same way an individual-seller purchase does.
- (constraint by ai) Trade offers are only possible on individual-seller listings explicitly flagged 'open to trade'; the flag is off by default. Rules out trade offers being available on every individual-seller listing or on any verified-business listing.
- (question by ai) Open: does an accepted trade offer generate any on-platform completion/closure state (mirroring the three-state purchase confirmation) or reputation signal (e.g. can trade counterparties review each other), or does it end at 'accepted' with everything after treated as off-platform? Not addressed by the Product Brief.
- (decision by ai) Business model: monetization is business-only at launch - commission on in-platform sales, possibly paired with a subscription tied to the verified badge, relationship undecided. Individual sellers are not charged; individual-seller monetization is deferred as an open strategic question, not resolved as a Non-goal (Product Brief Decision 4).
- (constraint by ai) Individual sellers are not charged any commission, listing, or subscription fee at launch. Rules out any individual-seller monetization mechanism in this iteration.
- (decision by ai) Non-goal additions from the Product Brief: exact commission percentage and the commission-vs-subscription relationship (Decision 4); a forfeitable buyer-protection deposit / any escrow-like hardening beyond the three-state confirmation, deferred to a possible v2 (Decision 9); the individual-seller monetization mechanism itself, deferred (Decision 4); a native mobile app, explicitly future/out of scope (Decision 13); offline functionality, not required (Decision 13).
- (decision by ai) Non-goal update: the prior Non-goal 'payment gateway/provider details are excluded' is superseded - there is no payment gateway at all now; the mechanism is the peer-to-peer settlement in CAP-20/its constraint above, not an unspecified detail of a gateway that exists.
- (event by ai) Self-validate pass 1 (coherence, Spec Law 1-6+8) run against the updated kernel (CAP-1..CAP-16 amended in place, CAP-17..CAP-25 newly assigned): PASS. Every capability has intent+success; intents stay WHAT not HOW (payment/commission/trade mechanics kept at the same behavioral altitude as the original 16, not implementation-level); every constraint rules something out (checked each against the decoration test); non-goals are explicit and now include the payment-gateway exclusion, commission-rate, buyer-protection-deposit, individual-seller-monetization, native-app, and offline items; CAP IDs 1-16 preserved unchanged in identity, CAP-17..CAP-25 newly assigned, none reused or renumbered.
- (event by ai) Self-validate pass 2 (preservation) walked spec-task-1/SPEC.md, JUSTIFICATION.md's Key Decisions, and the Product Brief/decisions.md claim by claim. Everything load-bearing landed in the kernel, a constraint, an assumption, or an open question above, except: the hand-authored Contract section in docs/spec-task-1/SPEC.md (deliberately outside this kernel - bmad-spec's five fields don't include a Contract field; Contract stays a human-authored artifact outside bmad-spec's output, flagged to the user) and narrative/GTM content from the Product Brief (positioning, competitive landscape, tone of voice, success-metric framing), which is brief-level strategy, not spec-kernel content, and stays in 01-product-brief.md rather than being duplicated into SPEC.md. No unintentional wrapper-only content dropped.
- (note by ai) CAP-23 intentionally not assigned: location/distance filtering was folded into CAP-1's update rather than given a standalone ID. Noting this explicitly so the CAP-22 to CAP-24 gap in the sequence isn't mistaken for a dropped or lost capability.
- (event by ai) SPEC.md re-rendered from this memlog: 24 capabilities (CAP-1..22, 24-25; CAP-23 intentionally unassigned), 22 constraints, 17 non-goals, 9 success-signal assertions, 8 assumptions (5 risky / 3 safe), 2 open questions. Companions: stakeholders.md (unchanged), plus two newly-declared adopted companions - docs/spec-task-1/SPEC.md (hand-authored Contract section, outside this kernel by design) and A-Product-Brief/01-product-brief.md (strategic/GTM layer). Spec finalized for this session; next step per course sequence is bmad-architecture, in a fresh session.
- (decision by ai) Human review (session 2): location/distance filtering applies to all listings (individual and verified-business), but its behavioral meaning differs by seller type - individual-seller listings use it for in-person pickup / local-casual-selling preference; verified-business listings use it as a proximity convenience filter only, not a pickup guarantee. Resolves the open question on filter scope.
- (constraint by ai) Location/distance filtering applies platform-wide across all listing types; only individual-seller listings carry the in-person-pickup/local-preference semantics attached to it. Rules out treating the filter as individual-seller-exclusive or as implying business pickup availability.
- (decision by ai) Human review: an accepted trade offer gets a lightweight mutual-completion confirmation - both buyer and seller independently confirm 'trade completed' before the trade is marked closed, no payment/comprobante steps involved (distinct from the three-state purchase confirmation, which does involve payment). No extension of the review/reputation system to trades was adopted. Resolves the open question on trade closure.
- (capability by ai) CAP-26 intent: Both parties to an accepted trade offer can independently confirm the trade was completed. success: A trade shows 'completed' only once both the buyer and the seller have separately confirmed it; an accepted trade with only one confirmation stays open.
- (decision by ai) Human review: a completed verified-business purchase (item-received-by-buyer) triggers a suggested, not automatic, add-to-collection prompt for the buyer - declining leaves no collection entry, accepting creates one with source=PlatformPurchase. Resolves the risky assumption on PurchaseListing/AddToCollection coupling.
- (capability by ai) CAP-27 intent: After a business-purchase order reaches item-received-by-buyer, the buyer is prompted to add the item to a collection, without it being created automatically. success: Accepting the prompt creates a collection entry with source=PlatformPurchase; declining or ignoring it creates none, and the order still shows as closed either way.
- (decision by ai) Human review: a bundle and an individual listing for the same underlying card by the same seller draw from one shared inventory pool - selling through either path decrements the same quantity, preventing overselling. Resolves the inventory-reconciliation half of the prior combined risky assumption; the individual-seller-to-business account transition half remains unresolved and stays flagged.
- (constraint by ai) A bundle's component card and that seller's individual listing of the same card (if any) share one inventory quantity; a sale through either path decrements the shared count. Rules out independently-tracked quantities for the same physical card across bundle and individual listings.
- (event by ai) Self-validate re-run after session-2 human review closed 4 of the standing gaps (location-filter scope, trade closure, purchase-to-collection coupling, bundle/individual inventory reconciliation). Coherence and preservation both re-checked: PASS. Remaining open items: individual-seller review-eligibility gate (deliberately left flagged per Martin's stage-4 request in spec-task-1, not revisited here), and the individual-seller-to-verified-business account transition mechanics (still genuinely unaddressed).
- (event by ai) SPEC.md re-rendered after session-2 gap closure: 27 capabilities (CAP-1..22,24..27; CAP-23 intentionally unused), 22 constraints, 15 non-goals, 12 success-signal bullets, 7 assumptions (4 risky, 3 safe), 0 open questions. bmad-spec step finalized; next step per course sequence is bmad-architecture, to run in a fresh session (trigger-mapping remains out of scope).
- (decision by ai) Peer-review remediation (Spec Task 2, session 3): user confirmed scope to fix B1/B2/B3/B6/D2/D3/D4/D5 plus sealed-product inventory ambiguity, per Martin's peer review of spec-task-1 cross-checked against this kernel. All resolutions below confirmed by user via AskUserQuestion (dir-structure/B6-capability/sealed-products) or accepted as directed phrasing fixes.
- (constraint by ai) B1 fix: the vague UGC-boundary constraint never ruled anything out. Replaced with a concrete rule: catalog and pricing data are platform-derived and never user-editable; only listings, reviews, and collection notes are user-generated content, and each is attributed to its authoring account. Rules out treating any catalog/price field as user-editable.
- (capability by ai) B2 fix: CAP-19 intent dropped the unverifiable adjective 'short' (peer review flagged it as unfalsifiable prose). New intent: 'A first-time individual seller completes a profile step before creating their first listing.' Exact fields remain a Non-goal (unchanged).
- (capability by ai) B3 fix: CAP-3 intent/success left 'historical trend' shape undefined, inconsistent with CAP-9's defined time series. Defined historical trend as a delta object (period, changePercent, referencePriceAtStart) - distinct from a full series - and noted both last-transaction-price and historical-trend are sourced from an external price-reference feed, not derived from platform transaction volume (ties to the D5 assumption below).
- (capability by ai) B6 fix (user-confirmed: add minimal capability, not a Non-goal): new CAP-28 - admin can hide/remove a listing or review that violates platform policy; hidden records stop appearing in browse/detail views but are retained for audit. Closes the moderation gap flagged by both the peer review and the architecture spine's own Deferred section.
- (assumption by ai) D2 fix: added Risky assumption - displaying Pokemon TCG names/artwork/imagery sourced from a third-party catalog API is assumed permissible for this course-project's use case; not verified as licensable for a commercial/production launch. Flagged, not resolved - IP/licensing review is out of scope for this iteration.
- (constraint by ai) D3 fix: added Constraint - legalIdentity data collected during business application is regulated personal data under Colombian Ley 1581 (habeas data); the platform must apply lawful-basis/consent and secure-handling practices to it. Specific mechanism (consent flow, retention, access requests) deferred to architecture/implementation - this constraint only establishes that the legal obligation exists and must be designed for.
- (constraint by ai) D4 fix: added Constraint with an informal NFR - catalog browse/filter queries target roughly 1-2s response for a catalog on the order of a few thousand to tens of thousands of entries (course-project scale). Explicitly not a load-tested SLA; exists so the spec isn't silent on performance the way the peer review flagged.
- (assumption by ai) D5 fix: added Risky assumption - last-transaction-price and historical-trend data are assumed to come from an external price-aggregation source, not derived from platform transaction history, since most sales (individual-seller) happen off-platform and business-sale volume will be near-zero at launch. The existence of a usable, licensable such source is not verified. Mirrors the architecture spine's own external-pricing decision, now reflected back into the kernel.
- (constraint by ai) Sealed-product inventory fix (user-confirmed: sealed products never bundle): added Constraint - sealed products are always listed individually and never participate as bundle components; only cards can be bundle components. Each sealed-product listing has its own independent inventory unit, with no shared-quantity reconciliation (unlike the card bundle/individual-listing sharing rule). Closes a gap AD-6 in the architecture spine didn't cover: InventoryUnit was keyed only by cardId with no sealed-product case.
- (note by ai) SellerNotVerified ambiguity check (mirrors peer-review Consistency finding ii): confirmed CAP-5/CAP-19 kernel text never implies SellerNotVerified applies to individual sellers - individual sellers have no verification gate at all, only the profile-completeness gate (IndividualSellerProfileIncomplete). No kernel wording change needed; the ambiguity lived only in the architecture spine's AD-11 error-ownership list, fixed there separately.
- (event) Self-validate re-run after peer-review remediation pass (session 3, Spec Task 2 prep): Pass 1 coherence PASS - CAP-28 has intent+success; CAP-3/CAP-4/CAP-19 rewording stays at WHAT altitude, no HOW leaked in; every new/edited constraint rules something out (habeas-data handling, sealed-product bundle exclusion, catalog/pricing non-editability, informal NFR); non-goals stay explicit (added moderation-policy-criteria exclusion). Pass 2 preservation PASS - all 9 confirmed peer-review findings (B1/B2/B3/B6/D2/D3/D4/D5 + sealed-product inventory) now land in the kernel; nothing dropped silently.
- (event) SPEC.md re-rendered after peer-review remediation: 28 capabilities (CAP-1..22, 24-28; CAP-23 intentionally unused), 30 constraints, 19 non-goals, 13 success-signal bullets, 9 assumptions (6 risky, 3 safe), 0 open questions. This is the version being carried into the Spec Task 2 deliverable package.
- (note by ai) Spec Task 2 deliverable-tree assembly (session 3, user-confirmed approach: create the exact required tree as a new deliverable layer, leave BMAD working-folder conventions untouched): ARCHITECTURE-SPINE.md copied in as a sibling of SPEC.md from its planning-artifacts run folder; companions frontmatter updated to reference it plus three new companion-files (glossary.md, persona-archetypes.md, two .mmd diagrams) about to be authored. 01-product-brief.md copied to design-artifacts/A-Product-Brief/project-brief.md per the assignment's required path.
- (note by ai) Phase transition: the entries below this line are the bmad-architecture run's audit trail, originally logged to its own append-only memlog at _bmad-output/planning-artifacts/architecture/architecture-Pok-mon-TCG-Marketplace-and-Collection-Management-2026-08-25/.memlog.md (a separate run folder outside this deliverable tree, per the assignment's own Context Window Isolation instruction to run bmad-spec and bmad-architecture in distinct sessions). Merged in verbatim, in original order, so this single file at the assignment's required path carries the complete AI audit trail end to end. The source file at the planning-artifacts path is left in place, unmodified, as the architecture run's own record.

## Architecture Phase — bmad-architecture (merged from a separate context-window run)

- (event by user) Run started. Working mode: Coaching path (user pre-selected, per class-exercise instructions). Deliverable scope: ARCHITECTURE-SPINE.md only, no additional deck/solution-doc (user confirmed, matching the course exercise's stated Output line).
- (note) Inputs read: SPEC.md (27 capabilities, 22 constraints, 15 non-goals, 12 success-signal bullets, 7 assumptions, 0 open questions), .memlog.md (spec decision trail), stakeholders.md, docs/spec-task-1/SPEC.md (Contract with 13 exact operation signatures), _bmad-output/A-Product-Brief/01-product-brief.md (GTM/positioning/tone layer). wds-2-trigger-mapping intentionally skipped, no B-Trigger-Map input exists.
- (decision by user) Design paradigm: modular monolith, hexagonal per module, in-process domain events for cross-module side effects (chosen over plain layered MVC and over microservices/message-bus). Binds: whole system. Prevents: silent coupling between parallel state systems (P2P orders, prepaid commission, trading, catalog/listing/binder) and infra cost/complexity incompatible with a 2-person free-tier team against a fixed Nov 2026 deadline.
- (decision by user) Stack/starter: Next.js (App Router, TypeScript) full-stack single deployable, tRPC for typed end-to-end procedures organized per domain module (mapping directly onto docs/spec-task-1/SPEC.md's 13 exact operation signatures), Prisma + PostgreSQL (Supabase or Neon free tier) for the relational model, Auth.js for sessions, Supabase Storage or Vercel Blob for comprobante uploads, deployed on Vercel free tier. Chosen over a split Next.js+NestJS two-deployable setup and over a Django/Python monolith.
- (version by ai) Web-verified (2026-08-25): Next.js 16.3.3 (Active LTS); tRPC v11.13.2; Prisma ORM 7.4.2 (stable/recommended for production -- Prisma 8 is still RC, not pinned); PostgreSQL via Supabase or Neon free tier; Tailwind CSS 4.3.3.
- (decision by ai) Auth substitution: Better Auth instead of Auth.js/NextAuth. Web research shows Auth.js has been in maintenance mode since early 2026 (security fixes only, v5 still on beta tag) and its own docs now point new projects to Better Auth (v1 stable since early 2025, self-hosted, stores user data in own DB, ~28k GitHub stars, used by MakerKit/Supastarter). Flagged to user as a research-driven adjustment to the earlier stack answer, not a silent swap.
- (decision by user) User confirmed (said 'continua' after reviewing all 7 invariant proposals + Better Auth swap, no pushback): Order 3-confirmation model, Commission/Listings ownership split with domain events, Trading as separate-but-analogous confirmation pattern reusing CAP-6 contact-message generation, single location-filter path with read-model annotation, shared InventoryUnit(sellerId,cardId) as sole quantity owner for bundle/individual-listing, Catalog/Listing/Binder three-module independence with tagged-union BinderEntry reference, no-offline/no-push/single-region scope pruning, and Better Auth over Auth.js. All adopted as-proposed.
- (decision by ai) Deployment/infra: Vercel (Next.js app + serverless tRPC handlers, free tier, git-push deploys, automatic PR preview environments) + single Supabase free-tier project (Postgres + Object Storage for comprobante uploads and card art) as the two managed services. No separate staging environment beyond Vercel's ephemeral PR previews, given 2-person team and free-tier budget; a second free Supabase project may be added later for local/dev isolation if needed, not now.
- (decision by ai) Module boundary enforcement at the data layer: one physical Postgres database, but table ownership mirrors module boundaries -- a module's Prisma models are written only by that module's own repository/application service; cross-module reads go through the owning module's public query API, never a raw cross-schema join or another module reaching into its tables directly. Prevents Prisma's single shared schema file from silently eroding the hexagonal module boundaries.
- (convention by ai) IDs: cuid2 string primary keys. Money: integer COP (no decimals) for all transactional/listing prices; USD/COP reference-price pairs (CAP-3) as a distinct value object, never merged with transactional money. Dates: stored UTC ISO 8601, rendered in America/Bogota at the presentation layer only. Domain errors: a shared DomainError code enum in shared-kernel carrying the Contract's exact error names (NotBusinessListing, IndividualSellerProfileIncomplete, NotVerifiedPurchaser, etc.), mapped by the tRPC error formatter -- modules throw/extend it, never ad-hoc strings. Domain events: past-tense names (OrderPaymentConfirmedByBusiness, CommissionBalanceExhausted, TradeAccepted, OrderClosed) on a simple in-process typed emitter, not a queue.
- (event by ai) Reviewer Gate dispatched: 3 input-reconciliation subagents (SPEC.md+stakeholders.md, spec-task-1 Contract, Product Brief) + rubric walker + the 2 configured finalize_reviewers (version-verification, adversarial-divergence). All 6 returned findings; full reviews written under reviews/. Findings triaged below; clear technical fixes applied inline, one business-risk finding (Vercel Hobby ToS vs. commercial/commission use) surfaced to user rather than silently resolved.
- (decision by ai) AD-1 amended: added collections->listings dependency edge (CAP-14 wishlist price/availability sort needs listings' query API); clarified 'public application-service interface' includes command methods (writes), not just queries -- resolves the AD-1/AD-8 tension the adversarial reviewer flagged around cross-module inventory writes.
- (decision by ai) AD-2 amended: pinned 'completed platform purchase' (CAP-7 review gate) = buyerItemReceivedConfirmedAt IS NOT NULL; made CAP-27's 'PurchaseListing never auto-creates a collection entry' an explicit AD rule (OrderClosed only triggers a dismissible prompt, AddToCollection stays separate buyer-initiated call).
- (decision by ai) AD-4 amended: openToTrade defaults false and is rejected on business listings (enforced in listings at the Listing aggregate, not trusted from trading); 'both confirmed' on TradeOffer is a computed property, never a stored status field; accepted trades now call listings.reserveInventory (AD-6) through the same path orders uses, closing the third-sale-path oversell gap the adversarial reviewer found.
- (decision by ai) AD-6 amended (adversarial reviewer Critical finding): inventory decrement moment pinned to Order/TradeOffer creation (reservation), not close -- prevents oversell under concurrent purchase+purchase or purchase+trade. Mechanism: listings.reserveInventory(inventoryUnitId, qty) is a synchronous command call sharing one Prisma transaction with the caller's row insert, not a domain event -- oversell correctness can't tolerate AD-10's eventual/best-effort event semantics.
- (decision by ai) AD-7 amended: added Listing.condition as a per-sale attribute distinct from CatalogEntry identity; made CollectionEntry.source (PlatformPurchase|Manual, CAP-12) an explicit always-surfaced required field.
- (decision by ai) New AD-9 added (adversarial reviewer Critical finding): domain events carry self-contained snapshot payloads (e.g. OrderClosed carries buyerId/businessId/item summary), so a subscriber never needs a synchronous back-call into the publisher for data it needs.
- (decision by ai) New AD-10 added (adversarial reviewer High finding): event dispatch semantics pinned -- publish only post-commit, in-process synchronous invocation of subscribers, listener errors caught+logged and never roll back or retry the publisher's committed transaction. No durable queue/retry at this scale; a missed side effect is a Deferred operational gap. Future retry paths must be idempotent (e.g. commission dedupes by orderId).
- (decision by ai) New AD-11 added (adversarial reviewer Medium finding): each DomainError code pinned to exactly one throwing module (explicit list in the AD); a module detecting another module's error condition propagates it unchanged rather than re-throwing its own copy.
- (decision by ai) CAP-3 clarified: 'last transaction price' and 'historical trend' are external reference data (e.g. PriceCharting/TCGplayer-style aggregator per Product Brief), ingested into catalog -- not derived from the platform's own orders records. Resolves the Contract-reconciliation reviewer's flagged module-path gap (GetCardDetail had no valid listings/catalog->orders edge) without adding a new dependency, and is consistent with SPEC.md's existing 'external catalog data source' risky assumption and price-algorithm non-goal.
- (version by ai) Stack table corrected against live web verification: tRPC 11.13.2 -> 11.18.x, Prisma 7.4.2 -> 7.6.x (noted @prisma/adapter-pg driver-adapter is now mandatory against Supabase Postgres, not optional). Added concrete Supabase free-tier caps (500MB DB/1GB storage/2-project limit/7-day auto-pause/no backup retention).
- (question by ai) Version-verification reviewer (HIGH severity): Vercel Hobby/free tier ToS reportedly restricts commercial/payment-processing use; this platform takes a commission so hosting it on Hobby may be non-compliant even though funds never flow through Vercel. NOT silently resolved -- flagged in the spine's Deferred section and surfaced directly to the user, since Vercel was a previously coached, user-confirmed stack choice. Revisit condition: team confirms Vercel's actual current Hobby ToS language and either accepts the risk, upgrades to Pro (~$20/mo, breaks all-free-tier constraint), or swaps hosting before commission flows go live.
- (note by ai) Added Deferred notes (not architecture decisions): admin moderation-of-listings/reviews has no operationalized capability in SPEC.md (pre-existing spec gap, carried forward not invented); AD-3/CAP-7's dependency on orders events means they transitively inherit the Product Brief's payments-may-slip-past-November timeline exemption -- flagged for epic/story sequencing.
- (note by ai) Added Structural Seed > Operations subsection (rubric walker finding: whole operational dimension was silent) -- logging via Vercel/Supabase built-ins, no error-tracking service pinned (Deferred), no backups on Supabase free tier (accepted trade-off), secrets via Vercel env vars. Added Consistency Conventions row naming AD-1/AD-8 boundary enforcement as PR-review-time (no automated linter adopted yet, Deferred).
- (event by ai) spine finalized: 11 ADs (AD-1..AD-11), lint clean, all 6 reviewer-gate findings triaged -- clear technical fixes applied inline; one business-risk finding (Vercel Hobby ToS vs. commission-taking commercial use) left open in Deferred and surfaced to the user rather than silently resolved. status set to final.
- (decision by ai) Spec Task 2 remediation pass (session 3): user confirmed (AskUserQuestion) to augment every AD-1..AD-11 additively with the assignment's required ADR fields (Context & Problem, Decision Taken, Status, Consequences & Trade-offs, Rejected Alternatives) on top of the existing Binds/Prevents/Rule core, not replace the terse format. Also confirmed two content fixes: AD-11 SellerNotVerified scope clarification, and AD-6 sealed-product inventory generalization (sealed products never bundle).
- (decision by ai) AD-11 fix (mirrors peer-review Consistency finding): clarified SellerNotVerified fires only for an unapproved-business account attempting to create/publish a listing; it never applies to an individual seller, whose only listing gate is IndividualSellerProfileIncomplete. The two error conditions were ambiguously adjacent in the original AD-11 list with no scope note.
- (decision by ai) AD-6 fix (user-confirmed: sealed products never bundle, closes a gap the peer-review process surfaced): generalized InventoryUnit's key from (sellerId, cardId) to (sellerId, itemRef), where itemRef is either a card (participates in the existing bundle/individual shared-inventory reconciliation) or a sealed product (always listed individually, its own independent InventoryUnit, no shared-quantity reconciliation logic -- simpler case, no cross-listing pooling since sealed products can never be a BundleComponent).
- (capability by ai) New AD-12 added: SPEC.md's new CAP-28 (admin listing/review moderation, added in the same remediation pass) needs architecture coverage -- the spine's own Deferred section previously called this an unoperationalized spec gap, which is now stale. AD-12: listings and reviews each own a nullable hiddenAt/hiddenReason pair on their own aggregate (Listing, Review); moderation hides, never deletes; all default browse/read queries filter hiddenAt IS NULL, only the admin moderation view includes hidden records; the hide/unhide command lives on the owning module's application service, invoked from an admin-only tRPC procedure gated by identity's isAdmin role -- no shared cross-module moderation table, consistent with AD-1/AD-8 ownership rules. Binds updated to add CAP-28; Deferred's stale moderation-gap note removed/updated to point at AD-12.
- (event by ai) ADR-field augmentation applied to AD-1 through AD-12: each now additionally carries Context & Problem, Decision Taken, Status (all Accepted -- none superseded), Consequences & Trade-offs, and Rejected Alternatives, layered on top of the existing terse Binds/Prevents/Rule core which remains unchanged as the build-facing summary. Satisfies the Spec Task 2 rubric's explicit ADR-field requirement without discarding the spine's original convergent format.
- (event by ai) Spec Task 2 remediation pass closed: spine re-distilled with AD-1..AD-12 (AD-12 new), every AD carrying the full ADR field set (Context & Problem, Decision Taken, Status, Consequences & Trade-offs, Rejected Alternatives) plus its original Binds/Prevents/Rule core. Frontmatter binds updated to include CAP-28. Deferred's stale admin-moderation gap note replaced with a pointer to AD-12. lint_spine.py: 0 findings. status remains final.

## Deliverable Assembly — back in the spec-run context

- (note by ai) End of merged architecture-phase entries. The remainder of this file continues the spec-run's own chronological log (deliverable-tree assembly, companion-file authoring, peer-review-remediation.md), picking back up after this merge point.
- (event by ai) Spec Task 2 deliverable package closed out (session 3): full required directory tree verified against the actual assignment PDF (task - spec 2.pdf) the user supplied -- design-artifacts/A-Product-Brief/project-brief.md, _bmad-output/specs/spec-pokemon-tcg-marketplace/{SPEC.md, ARCHITECTURE-SPINE.md, .memlog.md, companion-files/{glossary.md, persona-archetypes.md, diagrams/{system-context.mmd, state-transitions.mmd}}}, peer-review-remediation.md -- matches Section 4's required structure exactly (module names/domain content substituted for the Pokemon TCG project per the assignment's own 'or your own project' allowance). This .memlog.md was also merged with the previously-separate bmad-architecture run's memlog (entries logged under the 'Architecture Phase' heading above) so the single file at this required path now carries the complete AI audit trail across both WDS phases, per Deliverable F's requirement for one append-only decision log covering all agent interactions.
- (decision by ai) Added two explicit synthesis sections to ARCHITECTURE-SPINE.md -- 'System Boundaries & Dependencies' and 'State Mutation & Invariant Rules' -- between Design Paradigm and the numbered AD list, so the spine's headings literally match the assignment PDF's 6 required Deliverable-C section names (previously this content existed only distributed across individual ADs with no dedicated section). Also normalized SPEC.md heading casing ('Non-goals' to 'Non-Goals', 'Success signal' to 'Success Signal') to match the PDF's Deliverable-B section names exactly. Re-ran lint_spine.py: 0 findings, ok:true.
- (decision by ai) User-directed restructure (session 4): removed the top-level design-artifacts/ folder entirely (it held only one file, project-brief.md, plus several empty scaffold subfolders -- B-Trigger-Map, C-UX-Scenarios, D-Design-System, E-Development -- that were never populated). Renamed _bmad-output/A-Product-Brief/01-product-brief.md to _bmad-output/A-Product-Brief/project-brief.md (git mv) so there is a single canonical product-brief file, no duplicate copy. Updated all live references to the old filename: SPEC.md companions array + prose note, ARCHITECTURE-SPINE.md sources array, companion-files/persona-archetypes.md Sources line. Historical process records (dialog/decisions.md, dialog/progress-tracker.md, _progress/00-design-log.md) were left unmodified as-is since they are append-only/point-in-time records of what was produced during the original WDS run, matching the same non-destructive convention already applied to the architecture-run memlog. Trade-off accepted explicitly by the user: the deliverable tree no longer literally matches the assignment PDF's Section 4 path (design-artifacts/A-Product-Brief/project-brief.md) -- the product brief now lives only at _bmad-output/A-Product-Brief/project-brief.md. User chose this over keeping the PDF-literal duplicate path, prioritizing a single clean copy over literal-path submission compliance.
- (decision by ai) User-directed restructure (session 4, cont.): moved peer-review-remediation.md from repo root into docs/peer-review-remediation.md, alongside the peer-review PDF it responds to (docs/Martin's specification peer review.pdf) and Spec 1 (docs/spec-task-1/SPEC.md). Same trade-off as the product-brief move: the assignment PDF's Section 4 tree places this deliverable at the repo root, so this is a deliberate deviation from the literal required path, chosen by the user for a cleaner repo layout over exact submission-path compliance.
