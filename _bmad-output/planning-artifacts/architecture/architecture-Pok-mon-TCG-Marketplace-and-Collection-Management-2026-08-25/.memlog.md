---
scope: Whole-system architecture spine for the Pokemon TCG Marketplace and Collection Management Platform (Colombia), driven by SPEC-pokemon-tcg-marketplace
purpose: build-substrate
altitude: feature (keeps epics; next step is bmad-create-epics-and-stories)
updated: 2026-08-25T15:21
---

- (event by user) Run started. Working mode: Coaching path (user pre-selected, per class-exercise instructions). Deliverable scope: ARCHITECTURE-SPINE.md only, no additional deck/solution-doc (user confirmed, matching the course exercise's stated Output line).
- (note) Inputs read: SPEC.md (27 capabilities, 22 constraints, 15 non-goals, 12 success-signal bullets, 7 assumptions, 0 open questions), .memlog.md (spec decision trail), stakeholders.md, docs/spec-task-1/SPEC.md (Contract with 13 exact operation signatures), _bmad-output/A-Product-Brief/01-product-brief.md (GTM/positioning/tone layer). wds-2-trigger-mapping intentionally skipped, no B-Trigger-Map input exists.
- (decision by user) Design paradigm: modular monolith, hexagonal per module, in-process domain events for cross-module side effects (chosen over plain layered MVC and over microservices/message-bus). Binds: whole system. Prevents: silent coupling between parallel state systems (P2P orders, prepaid commission, trading, catalog/listing/binder) and infra cost/complexity incompatible with a 2-person free-tier team against a fixed Nov 2026 deadline.
- (decision by user) Stack/starter: Next.js (App Router, TypeScript) full-stack single deployable, tRPC for typed end-to-end procedures organized per domain module (mapping directly onto docs/spec-task-1/SPEC.md's 13 exact operation signatures), Prisma + PostgreSQL (Supabase or Neon free tier) for the relational model, Auth.js for sessions, Supabase Storage or Vercel Blob for comprobante uploads, deployed on Vercel free tier. Chosen over a split Next.js+NestJS two-deployable setup and over a Django/Python monolith.
- (version by ai) Web-verified (2026-08-25): Next.js 16.3.3 (Active LTS); tRPC v11.13.2; Prisma ORM 7.4.2 (stable/recommended for production -- Prisma 8 is still RC, not pinned); PostgreSQL via Supabase or Neon free tier; Tailwind CSS 4.3.3.
- (decision by ai) Auth substitution: Better Auth instead of Auth.js/NextAuth. Web research shows Auth.js has been in maintenance mode since early 2026 (security fixes only, v5 still on beta tag) and its own docs now point new projects to Better Auth (v1 stable since early 2025, self-hosted, stores user data in own DB, ~28k GitHub stars, used by MakerKit/Supastarter). Flagged to user as a research-driven adjustment to the earlier stack answer, not a silent swap.
- (decision by user) User confirmed (said 'continua' after reviewing all 7 invariant proposals + Better Auth swap, no pushback): Order 3-confirmation model, Commission/Listings ownership split with domain events, Trading as separate-but-analogous confirmation pattern reusing CAP-6 contact-message generation, single location-filter path with read-model annotation, shared InventoryUnit(sellerId,cardId) as sole quantity owner for bundle/individual-listing, Catalog/Listing/Binder three-module independence with tagged-union BinderEntry reference, no-offline/no-push/single-region scope pruning, and Better Auth over Auth.js. All adopted as-proposed.
- (decision by ai) Deployment/infra: Vercel (Next.js app + serverless tRPC handlers, free tier, git-push deploys, automatic PR preview environments) + single Supabase free-tier project (Postgres + Object Storage for comprobante uploads and card art) as the two managed services. No separate staging environment beyond Vercel's ephemeral PR previews, given 2-person team and free-tier budget; a second free Supabase project may be added later for local/dev isolation if needed, not now.
- (decision by ai) Module boundary enforcement at the data layer: one physical Postgres database, but table ownership mirrors module boundaries -- a module's Prisma models are written only by that module's own repository/application service; cross-module reads go through the owning module's public query API, never a raw cross-schema join or another module reaching into its tables directly. Prevents Prisma's single shared schema file from silently eroding the hexagonal module boundaries.
- (convention by ai) IDs: cuid2 string primary keys. Money: integer COP (no decimals) for all transactional/listing prices; USD/COP reference-price pairs (CAP-3) as a distinct value object, never merged with transactional money. Dates: stored UTC ISO 8601, rendered in America/Bogota at the presentation layer only. Domain errors: a shared DomainError code enum in shared-kernel carrying the Contract's exact error names (NotBusinessListing, IndividualSellerProfileIncomplete, NotVerifiedPurchaser, etc.), mapped by the tRPC error formatter -- modules throw/extend it, never ad-hoc strings. Domain events: past-tense names (OrderPaymentConfirmedByBusiness, CommissionBalanceExhausted, TradeAccepted, OrderClosed) on a simple in-process typed emitter, not a queue.
- (event by ai) Reviewer Gate dispatched: 3 input-reconciliation subagents (SPEC.md+stakeholders.md, spec-task-1 Contract, Product Brief) + rubric walker + the 2 configured finalize_reviewers (version-verification, adversarial-divergence). All 6 returned findings; full reviews written under reviews/. Findings triaged below; clear technical fixes applied inline, one business-risk finding (Vercel Hobby ToS vs. commercial/commission use) surfaced to user rather than silently resolved.
- (decision by ai) AD-1 amended: added collections->listings dependency edge (CAP-14 wishlist price/availability sort needs listings' query API); clarified 'public application-service interface' includes command methods (writes), not just queries -- resolves the AD-1/AD-8 tension the adversarial reviewer flagged around cross-module inventory writes.
- (decision by ai) AD-2 amended: pinned 'completed platform purchase' (CAP-7 review gate) = buyerItemReceivedConfirmedAt IS NOT NULL; made CAP-27's 'PurchaseListing never auto-creates a collection entry' an explicit AD rule (OrderClosed only triggers a dismissible prompt, AddToCollection stays separate buyer-initiated call).
- (decision by ai) AD-4 amended: openToTrade defaults false and is rejected on business listings (enforced in listings at the Listing aggregate, not trusted from trading); 'both confirmed' on TradeOffer is a computed property, never a stored status field; accepted trades now call listings.reserveInventory (AD-6) through the same path orders uses, closing the third-sale-path oversell gap the adversarial reviewer found.
- (decision by ai) AD-6 amended (adversarial reviewer Critical finding): inventory decrement moment pinned to Order/TradeOffer creation (reservation), not close -- prevents oversell under concurrent purchase+purchase or purchase+trade. Mechanism: listings.reserveInventory(inventoryUnitId, qty) is a synchronous command call sharing one Prisma transaction with the caller's row insert, not a domain event -- oversell correctness can't tolerate AD-10's eventual/best-effort event semantics.
- (decision by ai) AD-7 amended: added Listing.condition as a per-sale attribute distinct from CatalogEntry identity; made CollectionEntry.source (PlatformPurchase|Manual, CAP-12) an explicit always-surfaced required field.
- (decision by ai) New AD-9 added (adversarial reviewer Critical finding): domain events carry self-contained snapshot payloads (e.g. OrderClosed carries buyerId/businessId/item summary), so a subscriber never needs a synchronous back-call into the publisher for data it needs.
- (decision by ai) New AD-10 added (adversarial reviewer High finding): event dispatch semantics pinned -- publish only post-commit, in-process synchronous invocation of subscribers, listener errors caught+logged and never roll back or retry the publisher's committed transaction. No durable queue/retry at this scale; a missed side effect is a Deferred operational gap. Future retry paths must be idempotent (e.g. commission dedupes by orderId).
- (decision by ai) New AD-11 added (adversarial reviewer Medium finding): each DomainError code pinned to exactly one throwing module (explicit list in the AD); a module detecting another module's error condition propagates it unchanged rather than re-throwing its own copy.
- (decision by ai) CAP-3 clarified: 'last transaction price' and 'historical trend' are external reference data (e.g. PriceCharting/TCGplayer-style aggregator per Product Brief), ingested into catalog -- not derived from the platform's own orders records. Resolves the Contract-reconciliation reviewer's flagged module-path gap (GetCardDetail had no valid listings/catalog->orders edge) without adding a new dependency, and is consistent with SPEC.md's existing 'external catalog data source' risky assumption and price-algorithm non-goal.
- (version by ai) Stack table corrected against live web verification: tRPC 11.13.2 -> 11.18.x, Prisma 7.4.2 -> 7.6.x (noted @prisma/adapter-pg driver-adapter is now mandatory against Supabase Postgres, not optional). Added concrete Supabase free-tier caps (500MB DB/1GB storage/2-project limit/7-day auto-pause/no backup retention).
- (question by ai) Version-verification reviewer (HIGH severity): Vercel Hobby/free tier ToS reportedly restricts commercial/payment-processing use; this platform takes a commission so hosting it on Hobby may be non-compliant even though funds never flow through Vercel. NOT silently resolved -- flagged in the spine's Deferred section and surfaced directly to the user, since Vercel was a previously coached, user-confirmed stack choice. Revisit condition: team confirms Vercel's actual current Hobby ToS language and either accepts the risk, upgrades to Pro (~$20/mo, breaks all-free-tier constraint), or swaps hosting before commission flows go live.
- (note by ai) Added Deferred notes (not architecture decisions): admin moderation-of-listings/reviews has no operationalized capability in SPEC.md (pre-existing spec gap, carried forward not invented); AD-3/CAP-7's dependency on orders events means they transitively inherit the Product Brief's payments-may-slip-past-November timeline exemption -- flagged for epic/story sequencing.
- (note by ai) Added Structural Seed > Operations subsection (rubric walker finding: whole operational dimension was silent) -- logging via Vercel/Supabase built-ins, no error-tracking service pinned (Deferred), no backups on Supabase free tier (accepted trade-off), secrets via Vercel env vars. Added Consistency Conventions row naming AD-1/AD-8 boundary enforcement as PR-review-time (no automated linter adopted yet, Deferred).
- (event by ai) spine finalized: 11 ADs (AD-1..AD-11), lint clean, all 6 reviewer-gate findings triaged -- clear technical fixes applied inline; one business-risk finding (Vercel Hobby ToS vs. commission-taking commercial use) left open in Deferred and surfaced to the user rather than silently resolved. status set to final.
