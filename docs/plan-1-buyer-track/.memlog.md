---
topic: TEZG Buyer/Collector track PRD (Plan-1 course activity)
updated: 2026-09-05T20:08
---

- (decision by user) Protagonist confirmed as Valentina, 27, Bogota (Release-Driven Collector persona, dual buyer/individual-seller role) - user confirmed the plan's proposal
- (decision by user) FR-B3 comprobante upload NFR set to: upload completes within the same purchase session, under 10s on a typical mobile connection - SPEC.md has no number for this, user chose to set one rather than leave qualitative-only
- (decision by user) B4 dual-role scope limited to a summary My Sales tab (active listing count, pending contact/trade requests) in this PRD; full individual-seller listing management explicitly deferred as a Non-Goal to a future Seller/Business track PRD - user directed this be stated explicitly so a future agent expands it rather than assuming it's permanently out of scope
- (decision by user) Track scope: Buyer/Collector only for this PRD/exercise pass. Seller/Business track deliberately deferred - user is emailing the course instructor to confirm whether covering both tracks is valid given TEZG is a single app (unlike the course's two-app Host/Guest example); if approved, it will be an incremental second pass, not a rewrite
- (decision by user) Mobile declared the primary DESIGN target for this track's 4 scenarios, even though SPEC.md mandates equal-priority responsive (not mobile-only) - framed as a design-focus decision for this journey, not a deviation from the SPEC contract
- (decision by user) All 13 ADs in ARCHITECTURE-SPINE.md are treated as [ADOPTED] invariants for this PRD and will not be re-derived or re-litigated; only genuinely new track-specific open decisions get raised during the architecture phase
- (event) Drafted prd.md (UJ-1..4, FR-1..9, NFRs, Non-Goals, MVP, Success Metrics, Open Questions, Assumptions) and addendum.md (course-mapping table, rejected-alternative rationale, extended persona note)
- (event) Adversarial review of prd.md/addendum.md completed: 13 findings written to review-prd-adversarial.md (FR-9 trading/contact-request contradiction with Non-Goals; FR-2 zero-listing-under-location-filter contradicts SPEC Success Signal; FR-5 missing DomainError code; comprobante-visibility functional gap; unmeasurable SM-3; price-trust risk not surfaced; Collection/Binder scope silence; dual-role vs seller-exclusivity ambiguity; addendum broken cross-ref; others).
- (decision) Instructor confirmed covering both tracks is valid for a single-app product. Scope expanded: adding Seller/Business track (docs/plan-1-seller-track/) as an incremental pass, not a rewrite of this track. Protagonist structure confirmed via AskUserQuestion: two protagonists, 2+2 scenarios (Valentina continues as Individual Seller S1-S2, closing FR-9's deferred seam; new protagonist Andres covers Verified Business S3-S4). Comprobante-storage-ACL and Order-read-ownership open ADs are shared across tracks, resolved once in whichever track architects first.
- (event) Seller-track adversarial review (docs/plan-1-seller-track/review-prd-adversarial.md) surfaced 3 cross-track findings against this track's own prd.md: FR-9's dual-role Glossary entry still ambiguous when read standalone, FR-9's trade-request claim still uncited against the new FR-S3/FR-S4, and FR-9's [NOTE FOR PM] seam is now stale (Seller track exists). Candidates for a bmad-prd Update pass on FR-9 before/alongside this track's own triage.
- (decision) Triage: FR-2 Consequences reworded to cite SPEC.md Success Signal verbatim, fixing contradiction where zero-listing entries appeared excludable under a location filter (finding #4).
- (decision) Triage: FR-9 rewritten to drop ungrounded 'pending contact requests' claim, cite Seller-track FR-S1 (profile precondition) and FR-S3/FR-S4 (trade-offer data ownership), explain CAP-6 is stateless, and redirect Out of Scope/Notes to the now-existing Seller-track PRD (findings #1, #2, #13, cross-track #8, #10).
- (decision) Triage: Glossary 'Dual-role user' entry extended to explicitly distinguish seller-type exclusivity (SPEC.md Safe assumption) from buyer/individual-seller dual-role coexistence, citing seller-track prd.md §2.3 (finding #10, cross-track #9).
- (decision) Triage: §5 Non-Goals updated — Seller/Business bullet now reflects it's specified (not deferred); new bullet scopes Collection/Binder to FR-8's prompt only, full curation (CAP-9, CAP-11) explicitly out (finding #9).
- (decision) User decision (AskUserQuestion during triage): comprobante max file size = 5 MB per image, no resolution cap. Applied to FR-5 Feature-specific NFR, removed the [NOTE FOR PM] on undetermined size, resolved Open Question 2 (finding #12).
- (decision) Triage: §7 Security/Privacy NFR reframed — comprobante ACL is a shared cross-track architecture decision (cites seller-track FR-S7); asserted as a functional precondition (business must be able to read the comprobante) distinct from the ACL mechanism; flagged comprobante's own possible Ley 1581 exposure as distinct from AD-13's legalIdentity-only scope (findings #7, #8).
- (decision) Triage: §8 Open Questions rewritten — OQ2 (file size) and OQ4 (track-scope) marked Resolved with strikethrough; OQ3 appended with cross-track note; new OQ5 (comprobante Ley 1581 exposure) and OQ6 (missing DomainError code for FR-5's rejection case) added, both carried to the Architecture phase (findings #3, #8, #12; cross-track instructor-scope item).
- (decision) Triage: §9 SM-3 reworded qualitative/non-instrumented (no FR logs out-of-app contact, finding #6); new SM-4 added as an explicit risk flag — CAP-3 reference-price data is SPEC.md-flagged Risky, no metric measures price accuracy, accepted gap not remediated by this PRD (finding #5).
- (decision) Triage: consolidated four redundant restatements of 'individual-seller listings can't be purchased in-platform' (§2.2, Glossary, UJ-2, FR-4) — FR-4's Out of Scope is now the single source of truth, others cross-reference it (finding #14).
- (decision) Triage: Assumptions Index (§10) updated for FR-5's resolved file-size decision, FR-9's rewrite (new assumption on live trade-offer-count read mechanism, deferred to Architecture), and SM-4's accepted CAP-3 price-risk gap.
- (decision) Triage: fixed addendum.md's broken cross-reference (finding #11) — added an explicit 'Design-target decision' callout to prd.md §1 Vision stating mobile-primary-for-this-track, then corrected addendum's course-mapping table to cite that callout instead of a nonexistent §1 Vision claim.
- (event) Buyer-track PRD triage pass complete: all 14 adversarial-review findings (11 within-PRD + 3 cross-track) resolved via autofix, user decision, or deferred-with-carry-forward Open Question. Ready for Polish (Finalize step 5).
- (change) Polish: fixed two stale references left by the FR-9 rewrite — Glossary 'My Sales tab' entry no longer mentions the ungrounded 'pending contact requests,' and §6.2 no longer cites the removed [NOTE FOR PM] callout, both now pointing to FR-9's Notes / seller-track FR-S1-S4.
- (event) PRD finalized: status set to final, updated=2026-09-04. Artifacts: docs/plan-1-buyer-track/prd.md, addendum.md, review-prd-adversarial.md, .memlog.md (22 entries). Next: Seller-track PRD triage, then Phase 2 (wds-3-scenarios) for whichever track proceeds first.
- (change) Cross-track touch-up: corrected stale FR-S1..FR-S8 range references (FR-9 Notes, §5 Non-Goals) to FR-S1..FR-S9 after Seller-track triage added FR-S9. Trivial post-close factual correction, does not reopen scope.
- (decision) Phase 2 handover: design_intent=Dream Up (D) selected for all 4 scenarios by user, to keep momentum across both tracks' remaining phases; agent will draft each page wireframe/spec autonomously for user review.
- (event) Phase 2 (wds-3-scenarios) closed: 4 scenarios, 10 pages, quality Excellent across all dimensions. Artifacts: C-UX-Scenarios/00-ux-scenarios.md + 4 outlines + 10 page specs, _progress/00-design-log.md. Proceeding to Phase 3 (bmad-ux + wds-4-ux-design).
- (decision) Phase 3 (bmad-ux): visual direction = 'Trusted Ledger' (calm navy/cream, single muted teal accent, no gradients/shadows) chosen by user via AskUserQuestion over 'Holo Energy' and 'Binder Archive' alternatives, because TEZG has no inherited brand identity (whole-system Design System explicitly deferred) and price-trustworthiness is the trigger map's #1 driving force for this persona.
- (decision) Comprobante upload UI resolved as a standard HTML file input (accept=image/*), not a bespoke native camera integration -- derived from SPEC.md's explicit platform-wide constraint ruling out native camera/device-feature dependency, not a fresh Discovery question. OS-level chooser handles camera-vs-gallery.
- (event) Phase 3 Part A: DESIGN.md ('Trusted Ledger' visual identity — cream/navy/teal, no gradients/shadows, tabular price figures) and EXPERIENCE.md (10-surface IA, 5-state comprobante upload resolved as standard OS file-picker per SPEC.md constraint, 4 Key Flows) drafted via bmad-ux Fast path. Proceeding to Reviewer Gate.
- (decision) Reviewer Gate (Phase 3 Part A): 14 adversarial + 42 edge-case findings against DESIGN.md/EXPERIENCE.md. 3 genuine judgment calls surfaced to Martin via AskUserQuestion: (1) price-block reference-price shown as COP-converted equivalent only, not raw USD, to preserve tabular at-a-glance comparison; (2) abandoned/unpaid purchases (3.1 done, 3.2 never completed) now resume-able via Orders List listing unpaid orders with a 'Finish payment' action back to 3.2; (3) comprobante upload broadened to accept PDF alongside images (accept="image/*,application/pdf"), since bank receipts are often issued as PDFs -- Martin overrode my image-only recommendation here.
- (change) Applying ~20 direct fixes to DESIGN.md/EXPERIENCE.md from the Reviewer Gate: new payment-details-block component (3.1 QR/bank details, previously undesigned), Buy-vs-Contact-Seller CTA distinction on listing-card (2.1's actual climax mechanic, was undefined), contact-message outcome added to IA, NotBusinessListing error copy, empty-state component description reconciled between the two files, account-menu IA description corrected, trend +/- sign added to Accessibility Floor, comprobante state machine closed (OS-picker-cancel, change-photo-from-success, Confirm-I-Paid double-submit guard, 5MB+filetype disclosed upfront), order tri-state given concrete terminal/edge examples, Orders List now lists unpaid orders (ties to abandoned-purchase decision), general back-navigation/deep-link-guard/resize/zoom principles added to close remaining edge-case findings without over-specifying implementation.
- (assumption) Deferred (not fixed at spine level, logged as known gaps for Phase 3B page-specs / Phase 4 architecture): exact upload retry-limit/lockout count, HEIC transcoding handling, offline-vs-timeout error differentiation, zero-byte/corrupted file detection, desktop drag-and-drop/clipboard-paste support, background-tab/app-switch mid-upload behavior, live viewport resize reactivity details, browser zoom-reflow specifics, orientation-change handling, 2.1/3.1 explicit cancel-button placement (covered by general back-nav principle instead), My Sales tab listings-vs-trade-offers partial-empty sub-state copy. None of these block Phase 3B wireframing; each is a normal implementation-level or page-spec-level decision, not a spine-level gap.
- (event) Reviewer Gate resolved: all 14 adversarial + 42 edge-case findings triaged. ~25 fixes applied directly to DESIGN.md/EXPERIENCE.md (currency-COP-only price-block, Buy-vs-Contact-Seller CTA distinction, new payment-details-block component for 3.1, comprobante state machine closed end-to-end, order tri-state given 4 concrete worked examples incl. terminal/closed state, empty-state definitions reconciled across both files, account-menu IA description corrected, back-navigation/deep-link/resize/zoom general principles added). Remaining low-stakes implementation-level items logged as deferred (see prior assumption-type entry). Skipping bmad-ux's own key-screen-mock Finalize step -- the governing plan already assigns detailed wireframing to Phase 3 Part B (wds-4-ux-design/Freya, one pass per scenario), so producing HTML mocks here would duplicate that work rather than add value.
- (change) Polish (doc_standards): structural review found both spines sound (locked canonical order preserved, no redundancy warranting cuts after Reviewer Gate rewrites); prose review found one minor clarity fix (DESIGN.md Brand & Style: 'seller's/catalog's image content' -> 'card's own catalog image') and otherwise no editorial issues -- documents already tight after the adversarial/edge-case rewrite pass.
- (event) spines finalized
- (decision) Phase 3B (wds-4-ux-design/Freya, Dream mode) template adaptations: dropped bilingual SE/EN fields (product_languages=[en] only); repointed Spacing/Typography references from the nonexistent D-Design-System/00-design-system.md to this track's own DESIGN.md frontmatter tokens; used inline ASCII layout diagrams instead of raster sketch images (no image-generation tool invoked for this track).
- (decision) Mock coverage judgment for Phase 3B: pages 2.1 (individual-vs-business CTA contrast) and 3.2 (full comprobante state machine) promoted to dedicated wireframes per the governing plan's explicit call-out; remaining 8 pages judged spine-only sufficient (their page-spec tables fully determine layout/behavior with no ambiguity a visual reference would resolve).
- (change) Phase 3B page specs directly resolved 9 of the Phase 3A Reviewer Gate findings at the page-spec level: 2.1 now specifies the cta-business/cta-individual CTA contrast and gives the contact-message outcome (CAP-6) an inline surface; FR-4's NotBusinessListing rejection is prevented by design (individual listings never offer a Buy CTA) with a defensive race-condition state as fallback; 3.1's payment-details-block now has full accessibility treatment (QR is a convenience, never QR-only); 3.2 now discloses the 5MB/PDF cap upfront in Idle, adds a Change-file path back from Success, documents the OS-picker-cancel exit, and reconciles the Platform field away from the misleading '+ camera' label; 1.3 renders trend direction with a signed figure (not color-only); 1.2/1.3 correct DESIGN.md's Empty State misattribution (zero-listing tiles on 1.2 are tagged rows, not empty-state; the empty-state pattern lives solely on 1.3).
- (event) Phase 3 Part B complete: all 10 page specifications written to C-UX-Scenarios/, plus 2 dedicated wireframes (2.1, 3.2).
- (decision) Phase 3 review-gate follow-up judgment: existing review-ux-adversarial.md/review-ux-edge-cases.md (run against DESIGN.md/EXPERIENCE.md plus the scenario outlines and step-15 page stubs) are treated as sufficient coverage for the whole of Phase 3, including Part B's page specs -- Part B introduced no new product decisions beyond what those reviews already examined, and directly resolved 9 of their findings at the page-spec level rather than diverging from the reviewed spines. A fresh review pass on the finished page specs would mostly re-confirm fixes already applied; skipped to avoid redundant subagent spend, consistent with the Fast-path judgment already applied at Phase 3A's Polish step. Phase 3 deliverable checklist (DESIGN.md, EXPERIENCE.md, 10 page specs, >=2 wireframes, both reviews, triage in decision log) is now fully satisfied.
- (event) Phase 4 (bmad-architecture) activation: resolve_customization.py failed (Python 3.11+ required for tomllib); fell back to reading customize.toml directly per skill instructions. Loaded _bmad/bmm/config.yaml (user_name=Martin, communication_language=English). Fast path selected for decisions 2 (Order read-ownership) and 3 (dual-role identity) since both are already dictated by existing FRs/PRDs and precedent (AD-1 module boundaries, existing cookie-based non-enum role model) rather than genuinely open tradeoffs; decision 1 (comprobante storage/ACL mechanism) is a genuinely open tradeoff and is being surfaced to the user via AskUserQuestion rather than decided silently. Deliverable scope (ARCHITECTURE.md + diagram + 2 reviewer-gate reports) already fixed by the governing plan, so skipped re-asking Activation step 6's purpose/audience question.
- (decision) AD-14 (new, Phase 4): Comprobante read-access enforced via signed, time-limited URLs minted per-request by orders' application service (checked against Order.buyerId and the order's selling business), not convention-only (rejecting AD-13's weaker precedent) and not full server-proxied streaming. User choice via AskUserQuestion, citing that unlike AD-13's single-reader admin-only legalIdentity data, the comprobante has two legitimate readers (buyer + business) and is CAP-7's review-gate signal.
- (event) ARCHITECTURE.md drafted (status: draft internally, will flip to final after Reviewer Gate + triage): AD-14 (comprobante signed-URL ACL, per user's AskUserQuestion choice), AD-15 (Order read-ownership: buyer always, business from buyerPaidConfirmedAt onward -- resolves Seller-track FR-S7's own flagged [ASSUMPTION] in favor of its main FR text over its looser Consequences-bullet inference), AD-16 (dual-role identity for My Sales tab, derived server-side per project-context.md's existing non-enum role pattern, never client-asserted). Mermaid structural-seed diagram and Capability-to-Architecture map included. Next: Reviewer Gate (review-arch-adversarial.md, review-arch-edge-cases.md).
- (decision) AD-17: comprobante content extends AD-13's regulated-personal-data pattern (bank account/holder name) -- confirmed via AskUserQuestion, chose extend-AD-13 over accept-as-gap or redact-before-storage.
- (decision) AD-15 amended: business Order read-access persists indefinitely after closure (no end boundary) -- confirmed via AskUserQuestion over ends-at-closure or time-boxed alternatives.
- (decision) AD-14 amended: signed-URL TTL fixed at exactly 10 minutes (was stated as a 5-15 min range, flagged by both reviews as non-enforceable) -- confirmed via AskUserQuestion.
- (decision) Order.businessId is a stored column snapshotted at Order-creation time, not derived live via listing join -- confirmed via AskUserQuestion; resolves the load-bearing gap both AD-14 and AD-15 depended on. Moved out of Deferred into AD-15's Rule.
- (event) Reviewer Gate complete: review-arch-adversarial.md (12 findings) and review-arch-edge-cases.md (13 findings) written; 4 genuine tradeoffs surfaced to user via AskUserQuestion (Ley 1581 handling, post-closure access, TTL value, Order.businessId shape), all others triaged and fixed directly as mechanical/documentation gaps.
- (event) ARCHITECTURE.md amended post-Reviewer-Gate: added AD-17 (comprobante Ley 1581 handling), fixed AD-14 TTL to 10min fixed, amended AD-15 (stored businessId column, indefinite post-closure business access, abandoned-order note), added DomainError additions section (5 new orders-owned codes closing prd.md Q6 and Seller-track FR-S8 gaps), fixed Structural Seed diagram (added commission module, orders-listings edge), fixed AD-16 to exclude hiddenAt listings/offers, fixed binds namespace note, Stack/Consistency inheritance note, companion-not-yet-created caveat. status remains final -- Reviewer Gate + triage + fixes now actually precede it per the skill's own Finalize sequence.
- (event) Phase 4 (Architecture) closed for Buyer/Collector track. Deliverables: ARCHITECTURE.md (final, AD-14..17), review-arch-adversarial.md (12 findings), review-arch-edge-cases.md (13 findings), all triaged. Next: Phase 5 (bmad-check-implementation-readiness), then decision-log.md at track Cierre, then Seller/Business track Phases 2-5.
